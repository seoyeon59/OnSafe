<?xml version="1.0" encoding="utf-8"?>
<!--
    Pretendard 폰트 패밀리 정의.
    themes.xml에서 android:fontFamily="@font/pretendard" 로 앱 전체에 적용.
    각 weight는 textStyle="bold" → 700, 나머지는 400 기준으로 자동 매핑됨.
-->
<font-family xmlns:android="http://schemas.android.com/apk/res/android">
    <font
        android:font="@font/pretendard_regular"
        android:fontStyle="normal"
        android:fontWeight="400" />
    <font
        android:font="@font/pretendard_medium"
        android:fontStyle="normal"
        android:fontWeight="500" />
    <font
        android:font="@font/pretendard_semibold"
        android:fontStyle="normal"
        android:fontWeight="600" />
    <font
        android:font="@font/pretendard_bold"
        android:fontStyle="normal"
        android:fontWeight="700" />
</font-family>
