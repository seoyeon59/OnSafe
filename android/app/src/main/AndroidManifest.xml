<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OnSafe"
        tools:targetApi="31">

        <!-- 런처 -->
        <activity
            android:name="com.example.on_safe.ui.login.ModeSelectActivity"
            android:screenOrientation="portrait"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 메인 -->
        <activity
            android:name="com.example.on_safe.MainActivity"
            android:screenOrientation="portrait" />

        <!-- 로그인 플로우 -->
        <activity
            android:name="com.example.on_safe.ui.login.LoginActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name="com.example.on_safe.ui.login.FindIdActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name="com.example.on_safe.ui.login.FindPwActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name="com.example.on_safe.ui.login.RegisterStep1Activity"
            android:screenOrientation="portrait" />

        <activity
            android:name="com.example.on_safe.ui.login.RegisterStep2Activity"
            android:screenOrientation="portrait" />

        <activity
            android:name="com.example.on_safe.ui.login.PermissionActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name="com.example.on_safe.ResetPasswordActivity"
            android:screenOrientation="portrait" />

        <!-- 카메라 -->
        <activity
            android:name="com.example.on_safe.ui.camera.CameraModeActivity"
            android:screenOrientation="landscape" />

        <!-- 전체화면 -->
        <activity
            android:name="com.example.on_safe.ui.FullscreenActivity"
            android:screenOrientation="landscape" />

        <!-- 알림 내역 -->
        <activity
            android:name="com.example.on_safe.ui.notification.NotificationActivity"
            android:screenOrientation="portrait" />

        <!-- 설정 -->
        <activity
            android:name="com.example.on_safe.ui.settings.SettingsActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name="com.example.on_safe.ui.settings.EditProfileActivity"
            android:screenOrientation="portrait" />

    </application>

</manifest>
